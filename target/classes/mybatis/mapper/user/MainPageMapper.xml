<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.coffice.user.service.HomeImpl">


<select id="getList" resultType="com.coffice.dto.StoresDTO">
	SELECT *
	 FROM (SELECT Tb.*, rownum rNum FROM
	 	(SELECT * FROM stores join JJIM on stores.store_idx = jjim.store_idx 
where to_char(jjim_postdate,'yyyy-mm')=to_char(sysdate, 'yyyy-mm')
ORDER BY jjim_hit DESC)
	 		Tb
	 		)
		WHERE rNum BETWEEN 1 AND 6
</select>
</mapper>